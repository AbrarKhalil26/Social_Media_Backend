export enum GenderEnum {
  male = "male",
  female = "female",
}
export enum ProviderEnum {
  system = "system",
  google = "google",
}

export enum RoleEnum {
  admin = "admin",
  user = "user",
}

export enum FriendRequestEnum {
  pending = "pending",
  accepted = "accepted",
  rejected = "rejected",
}
